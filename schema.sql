CREATE TABLE "camera" ("uuid" VARCHAR PRIMARY KEY  NOT NULL  UNIQUE , "name" VARCHAR NOT NULL  UNIQUE ,"user" VARCHAR NOT NULL, "host" VARCHAR NOT NULL , "password" VARCHAR NOT NULL , "created_at" DATETIME NOT NULL  DEFAULT CURRENT_TIMESTAMP, "updated_at" DATETIME)